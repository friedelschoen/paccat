output {
    depends = [
        import ./textselect.pcr,
        import ./dwm.pcr
    ],

    script = ''
        mkdir {{out}}
        printenv > {{out}}/env
        echo '{{depends}}' > {{out}}/depends
    ''
}